import java.util.Random;

public class TreeBuilder {
    public static void CreateTree() {
        Random randomGenerator = new Random();
        int randomChildQuantity, m = 6, N = 200;
        while (N > 200) {
            randomChildQuantity = randomGenerator.nextInt(m);
        }
    }
}
